# Session notes

One file per working session: `<YYYY-MM-DD>-<topic>.md`.

This folder is the project's running memory. Start a session by reading the newest
file here; end it by making sure this session's file is complete. The template and
the rules live in `docs/WORKFLOW.md`.
